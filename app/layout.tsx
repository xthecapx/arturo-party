// Root layout - minimal wrapper for the entire app
// The actual layout with fonts and metadata is in [locale]/layout.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
